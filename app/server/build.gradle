archivesBaseName = "training-java-app-server"

eclipse {
    project {
        name = 'training-java-app-server'
    }
}

configurations {
    api {
        exclude group: "org.springframework.boot", module: "spring-boot-starter-data-jpa"
    }
}

dependencies {
    api project(':training-java-spring-framework')
}